name: ci

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout Chat Harness
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.2

      - name: Install dependencies
        run: bun install --ignore-scripts

      - name: Validate durable capability registry
        run: bun run validate

      - name: Run legacy capability tests
        run: bun run test

      - name: Capture Bun lockfile for migration
        run: |
          echo "__CHAT_HARNESS_BUN_LOCK_BEGIN__"
          cat bun.lock
          echo "__CHAT_HARNESS_BUN_LOCK_END__"

  browser-smoke:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout Chat Harness
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.2

      - name: Install dependencies
        run: bun install --ignore-scripts

      - name: Install Chromium
        run: bunx playwright install --with-deps --only-shell chromium

      - name: Run domain-agnostic browser runtime smoke test
        run: bun run smoke:browser
